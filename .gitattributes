# Point clouds are large binaries — track them with git-lfs.
# Run once:  git lfs install && git add .gitattributes
*.pcd filter=lfs diff=lfs merge=lfs -text
*.pcd.gz filter=lfs diff=lfs merge=lfs -text
